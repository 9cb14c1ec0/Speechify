<Window x:Class="Speechify.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Speechify"
        mc:Ignorable="d"
        Title="Speechify - OpenAI Speech to Text" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004578"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Speechify" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                    <TextBlock Text="Real-time Speech to Text using OpenAI GPT-4o Mini" 
                             FontSize="12" Foreground="#666666" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusText" Text="Disconnected" 
                             Foreground="#666666" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="#FF5252"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" Margin="20,20,20,10" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="ConnectButton" Content="Connect" 
                          Style="{StaticResource ModernButton}" 
                          Margin="0,0,10,0" Click="ConnectButton_Click"/>
                    
                    <Button x:Name="RecordButton" Content="Start Recording" 
                          Style="{StaticResource ModernButton}"
                          IsEnabled="False" Click="RecordButton_Click"/>
                </StackPanel>

                <TextBox x:Name="ApiKeyTextBox" Grid.Column="1" 
                       Margin="20,0" Padding="10" 
                       VerticalAlignment="Center"
                       BorderThickness="1" BorderBrush="#DDDDDD"
                       Tag="Enter your OpenAI API Key here..."
                       GotFocus="ApiKeyTextBox_GotFocus"
                       LostFocus="ApiKeyTextBox_LostFocus"
                       TextChanged="ApiKeyTextBox_TextChanged"/>

                <Button Grid.Column="2" Content="Clear" 
                      Style="{StaticResource ModernButton}"
                      Background="#6C757D" Click="ClearButton_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" Margin="20,10,20,20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#FAFAFA" 
                      BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" 
                      Padding="15,10" CornerRadius="8,8,0,0">
                    <TextBlock Text="Transcription Output" FontWeight="SemiBold" Foreground="#333333"/>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                            HorizontalScrollBarVisibility="Disabled" Padding="15">
                    <TextBox x:Name="TranscriptionTextBox" 
                           TextWrapping="Wrap" 
                           AcceptsReturn="True"
                           IsReadOnly="False"
                           BorderThickness="0"
                           Background="Transparent"
                           FontSize="14"
                           Foreground="#333333"
                           Text="Your transcribed text will appear here..."/>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="#333333" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="AudioLevelText" Text="Audio Level: " 
                             Foreground="White" VerticalAlignment="Center"/>
                    <ProgressBar x:Name="AudioLevelBar" Width="150" Height="10" 
                               Margin="5,0" VerticalAlignment="Center" Maximum="100"/>
                </StackPanel>

                <TextBlock Grid.Column="1" x:Name="ErrorText" 
                         Foreground="#FF5252" FontSize="12" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
